# BYD Vehicle API — Vercel Serverless Function
# Core: pyBYD library for BYD vehicle data access
pyBYD @ git+https://github.com/jkaberg/pyBYD.git

# ASGI web framework
fastapi>=0.100,<1

# ASGI server (used by Vercel Python runtime)
uvicorn>=0.24,<1

# Optional: better HTTP handling on Vercel
httpx>=0.25,<1

# Shared log queue backend (Vercel Redis / Upstash / any redis:// URL).
# Activated automatically when REDIS_URL (or a *_REST_URL + *_TOKEN pair) is set.
redis>=6,<9
